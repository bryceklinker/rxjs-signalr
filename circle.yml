machine:
  node: 
    version: 8.1.2
dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_AUTH_TOKEN" >> ~/.npmrc
deployment:
  npm:
    tag: /v[0-9]+(\.[0-9]+)*/
      commands:
    - npm publish
    - npm version
    - git push --follow-tags